# 스터디큐브 디자인 언어

> UI 를 쓰기 전에 이 문서를 먼저 읽는다.
> 바탕은 **Apple** 의 절제, 활력은 **Discord** 에서 빌린다. 둘을 섞는 게 아니라 **자리를 나눈다.**

---

## 0. 한 줄 원칙

**작업 화면은 조용하고 정확하게, 홈은 화려하게.**

이 앱은 하루 종일 켜두는 업무 도구다. 표와 격자를 여덟 시간 보는 사람에게 화려함은 피로다.
그래서 작업 화면(`/m/**`)은 Apple 쪽 — 악센트 하나, 그림자 없음, 여백으로 위계를 만든다.
반대로 홈(`/home`)과 학생·직원이 잠깐 들르는 화면은 하루에 몇 초 보는 곳이다.
거기서는 Discord 쪽 — 큰 글씨, 그라디언트, 움직임을 쓴다.

**섞지 마라.** 표 안에 그라디언트를 넣거나, 홈을 회색 표처럼 만들지 마라.

---

## 1. 층위와 정렬 (가장 자주 어기는 규칙)

> **상위 층위일수록 상하좌우 중앙정렬. 대칭감이 곧 "여기가 위층"이라는 신호다.**

| 층위 | 예 | 정렬 |
|---|---|---|
| 진입·선택 화면 | 홈, 직원 관리 진입, 학생 허브, 로그인 | **상하좌우 중앙.** 남는 세로를 위아래로 나눠 갖는다 |
| 작업 화면 | 좌석 배치도, 근무표, 도시락 월설정, 장부 | 가로 중앙 + 폭 상한. 세로는 위에서부터 |
| 밀도 화면 | 목록·표 | 가로 중앙 + 폭 상한. 왼쪽 정렬된 열 |

- 세로 중앙 정렬은 반드시 `justify-content: safe center` — 순수 `center` 는 내용이 넘칠 때 위가 잘리고 스크롤이 안 먹는다(겪은 결함).
- **폭 상한 없이 화면 끝까지 늘리지 마라.** 확정값: 도시락 월설정 950 · 목록 1080 · 근무표 980 · 대시보드 1080 · 스케쥴러 1400. 새 화면은 내용이 요구하는 최소 폭을 재서 정하고 근거를 남긴다.
- 헤더도 본문과 같은 폭. 헤더만 화면 끝까지 가면 축이 어긋나 보인다.
- **1280 과 1920 두 폭에서 확인**한다. 1920 에서만 티 나는 결함이 있다.

- **시선을 양쪽 끝으로 오가게 하지 마라.** 왼쪽 끝에 이름, 오른쪽 끝에 버튼처럼 관련된 것을 화면 양 끝에 벌려 놓으면 한 줄 읽을 때마다 눈이 화면을 가로지른다. 줄이 스무 개면 스무 번이다 — 이게 표를 오래 보면 피로한 진짜 이유다.
  - 관련된 것은 **가깝게 붙인다.** 행 안에서 이름과 그 행의 동작은 한 덩어리로.
  - 폭 상한이 있는 이유의 절반이 이것이다. 1920 에서 표를 끝까지 늘리면 왼쪽 이름과 오른쪽 버튼이 1미터쯤 떨어진다.
  - 화면 위쪽에 조작거리가 쌓이면 **본문이 아래로 밀려 안 보인다.** 그럴 땐 위가 아니라 **옆(사이드바)** 으로 보내라.

---

## 2. 색

기존 토큰(`globals.css`, `semantic-color.ts`)이 유일한 출처다. **새 색을 화면에 직접 박지 마라.**

- 악센트는 `--accent` **하나**다(#4F46E5). "누를 수 있다"는 신호는 전부 이 색. 두 번째 악센트를 들이지 마라.
- 상태색(성공·경고·위험)은 상태를 말할 때만. 장식으로 쓰지 마라.
- 같은 뜻 = 같은 색상각. 진함 = 확정, 연함 = 예정.
- **읽어야 하는 글자는 `--sub`**(6.59:1). `--faint` 는 placeholder·비활성처럼 흐려야 마땅한 것 전용이다.

### 홈에서만 허용하는 것
- 악센트를 축으로 한 그라디언트(예: `--accent` → 보라·자홍 쪽). **작업 화면에서는 금지.**
- 큰 숫자·큰 글씨. 지표 숫자는 크게 눌러도 된다.

---

## 3. 글자

- 한글이 주다. 영문 스택만 쓰지 마라 — Pretendard 계열 → `system-ui` 순으로 두고, 한글이 빠지면 자간·행간이 통째로 어긋난다.
- **큰 제목은 자간을 조인다**(-0.01em ~ -0.02em). 한글은 영문보다 이 효과가 더 크다. 12px 이하에는 쓰지 마라.
- 굵기 사다리: 400 / 600 / 700. **500 은 쓰지 않는다** — 400 과 구별이 안 돼 위계만 흐려진다.
- 본문 행간은 1.5 아래로 내리지 마라. 한글은 영문보다 행간이 더 필요하다.
- 숫자는 `font-variant-numeric: tabular-nums` — 표에서 자릿수가 흔들리면 못 읽는다.

---

## 4. 깊이

**그림자로 위계를 만들지 마라.** 위계는 (a) 바탕색이 바뀌는 것과 (b) 여백이 만든다.

| 단계 | 처리 | 쓰는 곳 |
|---|---|---|
| 평평 | 그림자·테두리 없음 | 구역 배경, 네비게이션 |
| 실선 한 겹 | 1px `--line` | 카드, 표 |
| 떠 있음 | `--shadow-lg` | 모달·시트·토스트 **뿐** |

카드와 버튼에 그림자를 두르지 마라. 카드가 스무 개면 그림자 스무 개가 서로를 지운다.

---

## 5. 묶음은 이름이 아니라 구분선으로

메뉴·목록에서 무리를 나눌 때 **제목을 붙이는 것이 첫 수단이 아니다.**

1. **구분선 또는 여백** — 이것으로 충분하면 여기서 끝낸다. 제목 없이도 무리는 보인다.
2. **2차 바** — 무리 안이 깊으면, 카테고리를 누를 때 옆으로 **좌→우로 밀려 나오는 바**에 하위 항목을 담는다.
3. 제목 글자는 마지막 수단이다. 무리마다 제목을 달면 화면이 글자로 덮인다.

- 빈 무리는 구분선째 없앤다.
- **화면이 없는 '준비중' 항목은 메뉴에 두지 마라.** 못 여는 문을 그려 두면 메뉴가 산발적으로 보인다.

---

## 6. 움직임

- 작업 화면: 상태가 바뀐 것을 알리는 정도. 120~180ms, 페이드·미세 이동.
- 홈·진입 화면: 스프링. 카드가 차례로 들어오는 정도까지 허용(`_demo/components/Motion.tsx` 참고).
- 누를 때 `transform: scale(0.97)` 한 종류로 통일.
- `prefers-reduced-motion` 을 존중한다.

---

## 7. 모양

- 반경 사다리: 8(칩·입력) / 12(카드) / 999(알약). **그 사이 값을 새로 만들지 마라.**
- 아이콘은 인라인 stroke SVG(`strokeWidth 1.6`, `currentColor`). **이모지 금지.**
- 축약형 `border`/`background` 와 조건부 개별 속성을 섞지 마라(렌더 깨짐 이력).

---

## 8. 이동

- **어느 화면에서든 홈으로 한 번에 갈 수 있어야 한다.** 좌측 레일 맨 위 로고가 홈이다.
- **홈에서 작업 화면으로도 한 번에.** 홈의 모듈 카드가 그 문이다.
- 뒤로 가기는 브라우저에 맡기지 말고 화면 안에 둔다(`backHref`).

---

## 9. 지표·그래프

- 분모가 정해진 지표(출결률·점유율)는 **축을 0~100%(또는 0~정원)로 고정**한다. 자동 스케일은 90석 중 1석을 만석으로 그린다(겪은 결함).
- 자동 스케일을 쓰면 **최댓값을 화면에 적는다.**
- 막대가 없으면 빈 축 대신 "아직 기록이 없습니다".
- 같은 지표가 여러 화면에 뜨면 정의를 한곳에 둔다(홈 90명 / 대시보드 89명으로 갈렸던 이력).
- 차트 라이브러리 금지. 인라인 SVG.

---

## 10. 하지 마라

- 두 번째 악센트 색을 들이는 것
- 카드·버튼·글자에 그림자
- 작업 화면의 장식 그라디언트
- 굵기 500
- 폭 상한 없는 화면
- 이모지
- 셀 안에 상태를 문장으로 설명하기(칩·점·빗금으로)
- 브라우저 `confirm()` — 되돌릴 수 있으면 실행 후 되돌리기, 아니면 화면 안 2단계 확인

---

출처: `npx getdesign@latest add apple` · `add discord` 두 분석을 이 앱에 맞게 갈라 정리했다.
